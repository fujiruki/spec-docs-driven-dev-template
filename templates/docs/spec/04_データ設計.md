# 04. データ設計

## DB設計

### テーブル一覧

| テーブル名 | 概要 |
|:--|:--|
| <!-- テーブル名 --> | <!-- 概要 --> |

### <!-- テーブル名 -->

| カラム | 型 | NULL | デフォルト | 説明 |
|:--|:--|:--|:--|:--|
| id | INTEGER | NO | AUTO | 主キー |
| <!-- カラム名 --> | <!-- 型 --> | <!-- YES/NO --> | <!-- デフォルト値 --> | <!-- 説明 --> |
| created_at | DATETIME | NO | CURRENT_TIMESTAMP | 作成日時 |
| updated_at | DATETIME | NO | CURRENT_TIMESTAMP | 更新日時 |

**インデックス:**
- <!-- インデックス定義（必要に応じて） -->

**リレーション:**
- <!-- 外部キー制約（必要に応じて） -->

---

## API設計

### エンドポイント一覧

| メソッド | パス | 概要 | 認証 |
|:--|:--|:--|:--|
| GET | `/api/resources` | リソース一覧取得 | <!-- 要/不要 --> |
| GET | `/api/resources/:id` | リソース詳細取得 | <!-- 要/不要 --> |
| POST | `/api/resources` | リソース作成 | <!-- 要/不要 --> |
| PUT | `/api/resources/:id` | リソース更新 | <!-- 要/不要 --> |
| DELETE | `/api/resources/:id` | リソース削除 | <!-- 要/不要 --> |

### リクエスト/レスポンス例

#### GET `/api/resources`

**レスポンス:**
```json
{
  "data": [
    {
      "id": 1
    }
  ]
}
```

---

## 型定義

<!-- フロントエンドで使用する型定義 -->

```typescript
interface Resource {
  id: number;
  // ...
}
```
