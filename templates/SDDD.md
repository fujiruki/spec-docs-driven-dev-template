# SdDD 共通運用規則

> このファイルはSdDDのツール非依存な正本である。Claude Code、Codex、その他のAIは、作業前にこのファイルと今回の要望に関係する仕様を読む。

## 目的

SdDDは、AIに読ませる運用規則とMarkdownテンプレートをセットにした開発手法である。利用者が要望を伝え、必要な判断に答える。AIまたは自動化が、要望の記録、仕様化、タスク化、実装、検証、引継ぎを進める。

## 6つのコア規則

1. 会話または `docs/requests.md` で受けた要望は、仕様の話を始める前に、プロジェクトへ保存してよい範囲の原文を残す。仕様化を開始する要望にはR-IDを付ける
2. 現在有効な振る舞いは `docs/spec/` を正とし、仕様にない実装をしない
3. 仕様が確定した時、原文、要望ID、仕様リンク、受け入れ条件を `docs/requests_log.md` に記録する
4. 同じ時に、`docs/spec/`、`docs/SPEC.md`、`task.md` または `tasks/R-xxxx.md` を更新してから、`docs/requests.md` の該当項目だけを削除する
5. 実装、検証、完了、保留、見送り、取り下げ、他要望への統合は、同じ要望台帳記録の状態と根拠を更新する。原文と過去の判断を消さない
6. AI・担当者が交代しても、要望ID、仕様、タスク、検証、引継書から再開できる状態を保つ

## 要望ID

- 形式は `R-0001` の4桁連番を使う
- 新しいIDを付ける前に、`docs/requests.md`、`docs/requests_log.md`、分割済みなら `docs/requests_log/` を確認し、最大番号の次を採番する
- IDは削除・再利用・別要望への付け替えをしない。訂正・取り下げ・他要望への統合も同じIDの記録を更新する
- 類似要望は勝手に消さない。重複と判断した場合も、対象R-IDと理由を記録し、発注者に確認する

## 正本の境界

| 情報 | 正本 |
|:--|:--|
| 未整理・仕様確認中の要望 | `docs/requests.md` |
| 仕様確定後の原文・状態・経緯 | `docs/requests_log.md` |
| 現在有効な仕様 | `docs/spec/` |
| 実装・検証計画 | `task.md` または `tasks/R-xxxx.md` |
| チームの担当・議論 | GitHub Issue（使う場合） |
| 差分・CI・レビュー・統合 | GitコミットとPull Request（使う場合） |

SQLite、HTML、ダッシュボード、CodeGraphは検索・表示・影響調査の補助であり、Markdownの正本を上書きしない。

## 利用者とAIの役割

### 利用者がすること

1. 要望を `docs/requests.md` に書く、またはAIへ伝える
2. AIが質問した、仕様上の判断が必要な点に答える
3. 必要な結果確認、マージ、公開の承認をする

### AI・自動化がすること

1. 会話で受けた要望を原文で記録し、仕様化を始める時に要望IDを採番する
2. 必要な確認だけを行い、仕様を確定する
3. 仕様、台帳、タスク、変更履歴を整合させる
4. 要望ID、仕様、受け入れ条件、検証方法を実装担当へ渡す
5. 実装、テスト、レビュー、引継ぎ、要望台帳の状態更新を進める

単独実装では同じAIまたは人が兼任してよい。その場合も、実装前に仕様・台帳・タスクを整え、実装後に検証結果を残す。

## 処理手順

### 仕様が未確定の時

- `docs/requests.md` に残す
- 仕様化を始める要望には、このファイルの採番規則でR-IDを付ける
- 曖昧な点、選択肢、影響を具体的に確認する
- 勝手に実装、削除、見送り、他要望への統合をしない

### 仕様が確定した時

1. `docs/requests_log.md` に要望ID、原文、状態 `仕様化済み`、仕様リンク、受け入れ条件を追記する
2. `docs/spec/`、`docs/SPEC.md`、必要なら変更履歴を更新する
3. `task.md` または要望ID別タスクを作成する
4. 記録を確認してから `docs/requests.md` の該当項目だけを削除する

### 実装と完了

実装担当には、要望ID、仕様、受け入れ条件、検証方法、変更可能な範囲、タスク更新箇所を渡す。実装開始時に台帳を `実装中`、確認時に `検証中`、結果確定時に `完了`・`保留`・`見送り`・`取り下げ`・`他要望へ統合` のいずれかへ更新する。

`他要望へ統合` は、別のR-IDへ作業を統合した時だけ使う。統合先R-IDと理由を必ず残す。コードがマージされたことは状態 `完了` の根拠として記録し、`他要望へ統合` とは混同しない。

## 公開範囲と安全

- `requests.md` と `requests_log.md` は通常Git管理される。リポジトリの公開範囲を確認してから記録する
- パスワード、トークン、秘密鍵、個人情報、契約上の秘匿情報、公開できない会話原文をMarkdown・Issue・PRへ記録しない
- 秘匿情報を含む要望は、発注者と安全な保管先を確認し、リポジトリには公開可能な要約と参照先だけを残す
- 公開IssueやPRには、生の会話ではなく必要最小限の要約を記録する

## 任意の運用モジュール

- worktree、複数Agent、GitHub、PR、Issueを使う時は `docs/collaboration.md` を読む
- 自動化、SQLite索引、HTML表示、CodeGraphを使う時は `docs/automation.md` を読む
- 複数モジュールをまたぐ変更や影響範囲が読みにくい変更では、CodeGraphで入口・呼び出し関係・影響候補を調べ、実際に確認するファイルとテストをタスクへ記録する
- CodeGraphのローカル索引 `.codegraph/` とSdDDのローカル導入情報 `.sddd/` は既定でGit管理しない。共有が必要な設定・生成物だけをチームで明示的に選ぶ

## 引継ぎ

重要な判断後、一定のサブタスク完了後、同じ失敗が繰り返される時、セッションを替える前には引継書またはチェックポイントを作る。

引継書には、要望ID、ブランチ・worktree・コミットSHA、未コミット変更、読んだ仕様、テスト、既知の問題、次の一手を残す。新担当はそれを読んで理解したスコープと最初の検証を報告してから再開する。
